version: "3.8"

services:
  app:
    build: .
    container_name: sensor_monitor
    ports:
      - "8000:8000"
    volumes:
      - ./sensor_monitor.db:/app/sensor_monitor.db
    environment:
      - DATABASE_URL=sqlite:///./sensor_monitor.db

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - app

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
